/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from "../runtime";
/**
 *
 * @export
 * @interface ClientJobStatus
 */
export interface ClientJobStatus {
    /**
     *
     * @type {number}
     * @memberof ClientJobStatus
     */
    contentLength?: number;
    /**
     *
     * @type {string}
     * @memberof ClientJobStatus
     */
    digestAlgo?: string;
    /**
     *
     * @type {string}
     * @memberof ClientJobStatus
     */
    digestHex?: string;
    /**
     *
     * @type {number}
     * @memberof ClientJobStatus
     */
    eventCount?: number;
    /**
     *
     * @type {string}
     * @memberof ClientJobStatus
     */
    fileLink?: string;
    /**
     *
     * @type {string}
     * @memberof ClientJobStatus
     */
    jobId: string;
    /**
     *
     * @type {string}
     * @memberof ClientJobStatus
     */
    jobUrl?: string;
    /**
     *
     * @type {string}
     * @memberof ClientJobStatus
     */
    message?: string;
    /**
     *
     * @type {number}
     * @memberof ClientJobStatus
     */
    percentComplete?: number;
    /**
     *
     * @type {number}
     * @memberof ClientJobStatus
     */
    resultCount?: number;
    /**
     *
     * @type {number}
     * @memberof ClientJobStatus
     */
    runDuration?: number;
    /**
     *
     * @type {string}
     * @memberof ClientJobStatus
     */
    status?: string;
}

/**
 * Check if a given object implements the ClientJobStatus interface.
 */
export function instanceOfClientJobStatus(value: object): boolean {
    let isInstance = true;
    isInstance = isInstance && "jobId" in value;

    return isInstance;
}

export function ClientJobStatusFromJSON(json: any): ClientJobStatus {
    return ClientJobStatusFromJSONTyped(json, false);
}

export function ClientJobStatusFromJSONTyped(json: any, ignoreDiscriminator: boolean): ClientJobStatus {
    if (json === undefined || json === null) {
        return json;
    }
    return {
        contentLength: !exists(json, "content_length") ? undefined : json["content_length"],
        digestAlgo: !exists(json, "digest_algo") ? undefined : json["digest_algo"],
        digestHex: !exists(json, "digest_hex") ? undefined : json["digest_hex"],
        eventCount: !exists(json, "event_count") ? undefined : json["event_count"],
        fileLink: !exists(json, "file_link") ? undefined : json["file_link"],
        jobId: json["job_id"],
        jobUrl: !exists(json, "job_url") ? undefined : json["job_url"],
        message: !exists(json, "message") ? undefined : json["message"],
        percentComplete: !exists(json, "percent_complete") ? undefined : json["percent_complete"],
        resultCount: !exists(json, "result_count") ? undefined : json["result_count"],
        runDuration: !exists(json, "run_duration") ? undefined : json["run_duration"],
        status: !exists(json, "status") ? undefined : json["status"],
    };
}

export function ClientJobStatusToJSON(value?: ClientJobStatus | null): any {
    if (value === undefined) {
        return undefined;
    }
    if (value === null) {
        return null;
    }
    return {
        content_length: value.contentLength,
        digest_algo: value.digestAlgo,
        digest_hex: value.digestHex,
        event_count: value.eventCount,
        file_link: value.fileLink,
        job_id: value.jobId,
        job_url: value.jobUrl,
        message: value.message,
        percent_complete: value.percentComplete,
        result_count: value.resultCount,
        run_duration: value.runDuration,
        status: value.status,
    };
}
