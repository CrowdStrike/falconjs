/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from "../runtime";
import type { DomainControlBenchmark } from "./DomainControlBenchmark";
import { DomainControlBenchmarkFromJSON, DomainControlBenchmarkFromJSONTyped, DomainControlBenchmarkToJSON } from "./DomainControlBenchmark";

/**
 *
 * @export
 * @interface DomainControl
 */
export interface DomainControl {
    /**
     *
     * @type {Array<DomainControlBenchmark>}
     * @memberof DomainControl
     */
    benchmarks?: Array<DomainControlBenchmark>;
    /**
     *
     * @type {string}
     * @memberof DomainControl
     */
    description?: string;
    /**
     *
     * @type {string}
     * @memberof DomainControl
     */
    framework?: string;
    /**
     *
     * @type {string}
     * @memberof DomainControl
     */
    name?: string;
    /**
     *
     * @type {string}
     * @memberof DomainControl
     */
    section?: string;
    /**
     *
     * @type {string}
     * @memberof DomainControl
     */
    version?: string;
}

/**
 * Check if a given object implements the DomainControl interface.
 */
export function instanceOfDomainControl(value: object): value is DomainControl {
    return true;
}

export function DomainControlFromJSON(json: any): DomainControl {
    return DomainControlFromJSONTyped(json, false);
}

export function DomainControlFromJSONTyped(json: any, ignoreDiscriminator: boolean): DomainControl {
    if (json == null) {
        return json;
    }
    return {
        benchmarks: json["benchmarks"] == null ? undefined : (json["benchmarks"] as Array<any>).map(DomainControlBenchmarkFromJSON),
        description: json["description"] == null ? undefined : json["description"],
        framework: json["framework"] == null ? undefined : json["framework"],
        name: json["name"] == null ? undefined : json["name"],
        section: json["section"] == null ? undefined : json["section"],
        version: json["version"] == null ? undefined : json["version"],
    };
}

export function DomainControlToJSON(value?: DomainControl | null): any {
    if (value == null) {
        return value;
    }
    return {
        benchmarks: value["benchmarks"] == null ? undefined : (value["benchmarks"] as Array<any>).map(DomainControlBenchmarkToJSON),
        description: value["description"],
        framework: value["framework"],
        name: value["name"],
        section: value["section"],
        version: value["version"],
    };
}
