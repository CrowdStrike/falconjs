/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from "../runtime";
import type { Hour } from "./Hour";
import { HourFromJSON, HourFromJSONTyped, HourToJSON } from "./Hour";
import type { TenMinutes } from "./TenMinutes";
import { TenMinutesFromJSON, TenMinutesFromJSONTyped, TenMinutesToJSON } from "./TenMinutes";
import type { Day } from "./Day";
import { DayFromJSON, DayFromJSONTyped, DayToJSON } from "./Day";
import type { OneMinute } from "./OneMinute";
import { OneMinuteFromJSON, OneMinuteFromJSONTyped, OneMinuteToJSON } from "./OneMinute";

/**
 *
 * @export
 * @interface QuerySpent
 */
export interface QuerySpent {
    /**
     *
     * @type {Day}
     * @memberof QuerySpent
     */
    day: Day;
    /**
     *
     * @type {Hour}
     * @memberof QuerySpent
     */
    hour: Hour;
    /**
     *
     * @type {OneMinute}
     * @memberof QuerySpent
     */
    oneMinute: OneMinute;
    /**
     *
     * @type {TenMinutes}
     * @memberof QuerySpent
     */
    tenMinutes: TenMinutes;
}

/**
 * Check if a given object implements the QuerySpent interface.
 */
export function instanceOfQuerySpent(value: object): value is QuerySpent {
    if (!("day" in value) || value["day"] === undefined) return false;
    if (!("hour" in value) || value["hour"] === undefined) return false;
    if (!("oneMinute" in value) || value["oneMinute"] === undefined) return false;
    if (!("tenMinutes" in value) || value["tenMinutes"] === undefined) return false;
    return true;
}

export function QuerySpentFromJSON(json: any): QuerySpent {
    return QuerySpentFromJSONTyped(json, false);
}

export function QuerySpentFromJSONTyped(json: any, ignoreDiscriminator: boolean): QuerySpent {
    if (json == null) {
        return json;
    }
    return {
        day: DayFromJSON(json["day"]),
        hour: HourFromJSON(json["hour"]),
        oneMinute: OneMinuteFromJSON(json["oneMinute"]),
        tenMinutes: TenMinutesFromJSON(json["tenMinutes"]),
    };
}

export function QuerySpentToJSON(value?: QuerySpent | null): any {
    if (value == null) {
        return value;
    }
    return {
        day: DayToJSON(value["day"]),
        hour: HourToJSON(value["hour"]),
        oneMinute: OneMinuteToJSON(value["oneMinute"]),
        tenMinutes: TenMinutesToJSON(value["tenMinutes"]),
    };
}
