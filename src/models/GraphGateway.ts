/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from "../runtime";
import type { Flows } from "./Flows";
import { FlowsFromJSON, FlowsFromJSONTyped, FlowsToJSON } from "./Flows";

/**
 *
 * @export
 * @interface GraphGateway
 */
export interface GraphGateway {
    /**
     *
     * @type {Flows}
     * @memberof GraphGateway
     */
    flows: Flows;
    /**
     *
     * @type {string}
     * @memberof GraphGateway
     */
    nodeID: string;
    /**
     * The type of gateway being specified, allowed values are; exclusive, inclusive and parallel.
     * @type {string}
     * @memberof GraphGateway
     */
    type: string;
}

/**
 * Check if a given object implements the GraphGateway interface.
 */
export function instanceOfGraphGateway(value: object): value is GraphGateway {
    if (!("flows" in value) || value["flows"] === undefined) return false;
    if (!("nodeID" in value) || value["nodeID"] === undefined) return false;
    if (!("type" in value) || value["type"] === undefined) return false;
    return true;
}

export function GraphGatewayFromJSON(json: any): GraphGateway {
    return GraphGatewayFromJSONTyped(json, false);
}

export function GraphGatewayFromJSONTyped(json: any, ignoreDiscriminator: boolean): GraphGateway {
    if (json == null) {
        return json;
    }
    return {
        flows: FlowsFromJSON(json["flows"]),
        nodeID: json["nodeID"],
        type: json["type"],
    };
}

export function GraphGatewayToJSON(value?: GraphGateway | null): any {
    if (value == null) {
        return value;
    }
    return {
        flows: FlowsToJSON(value["flows"]),
        nodeID: value["nodeID"],
        type: value["type"],
    };
}
