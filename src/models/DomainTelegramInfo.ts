/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { DomainTelegramChannelInfo } from './DomainTelegramChannelInfo';
import {
    DomainTelegramChannelInfoFromJSON,
    DomainTelegramChannelInfoFromJSONTyped,
    DomainTelegramChannelInfoToJSON,
} from './DomainTelegramChannelInfo';
import type { DomainTelegramSenderInfo } from './DomainTelegramSenderInfo';
import {
    DomainTelegramSenderInfoFromJSON,
    DomainTelegramSenderInfoFromJSONTyped,
    DomainTelegramSenderInfoToJSON,
} from './DomainTelegramSenderInfo';
import type { DomainTelegramRecipientInfo } from './DomainTelegramRecipientInfo';
import {
    DomainTelegramRecipientInfoFromJSON,
    DomainTelegramRecipientInfoFromJSONTyped,
    DomainTelegramRecipientInfoToJSON,
} from './DomainTelegramRecipientInfo';

/**
 * 
 * @export
 * @interface DomainTelegramInfo
 */
export interface DomainTelegramInfo {
    /**
     * 
     * @type {DomainTelegramChannelInfo}
     * @memberof DomainTelegramInfo
     */
    channelInfo?: DomainTelegramChannelInfo;
    /**
     * 
     * @type {DomainTelegramRecipientInfo}
     * @memberof DomainTelegramInfo
     */
    recipientInfo?: DomainTelegramRecipientInfo;
    /**
     * 
     * @type {DomainTelegramSenderInfo}
     * @memberof DomainTelegramInfo
     */
    senderInfo?: DomainTelegramSenderInfo;
}

/**
 * Check if a given object implements the DomainTelegramInfo interface.
 */
export function instanceOfDomainTelegramInfo(value: object): value is DomainTelegramInfo {
    return true;
}

export function DomainTelegramInfoFromJSON(json: any): DomainTelegramInfo {
    return DomainTelegramInfoFromJSONTyped(json, false);
}

export function DomainTelegramInfoFromJSONTyped(json: any, ignoreDiscriminator: boolean): DomainTelegramInfo {
    if (json == null) {
        return json;
    }
    return {
        
        'channelInfo': json['channel_info'] == null ? undefined : DomainTelegramChannelInfoFromJSON(json['channel_info']),
        'recipientInfo': json['recipient_info'] == null ? undefined : DomainTelegramRecipientInfoFromJSON(json['recipient_info']),
        'senderInfo': json['sender_info'] == null ? undefined : DomainTelegramSenderInfoFromJSON(json['sender_info']),
    };
}

export function DomainTelegramInfoToJSON(value?: DomainTelegramInfo | null): any {
    if (value == null) {
        return value;
    }
    return {
        
        'channel_info': DomainTelegramChannelInfoToJSON(value['channelInfo']),
        'recipient_info': DomainTelegramRecipientInfoToJSON(value['recipientInfo']),
        'sender_info': DomainTelegramSenderInfoToJSON(value['senderInfo']),
    };
}

