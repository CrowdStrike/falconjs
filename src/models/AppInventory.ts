/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from "../runtime";
import type { ErrorAppInventory } from "./ErrorAppInventory";
import { ErrorAppInventoryFromJSON, ErrorAppInventoryFromJSONTyped, ErrorAppInventoryToJSON } from "./ErrorAppInventory";
import type { AppAppInventory } from "./AppAppInventory";
import { AppAppInventoryFromJSON, AppAppInventoryFromJSONTyped, AppAppInventoryToJSON } from "./AppAppInventory";
import type { MetaAppInventory } from "./MetaAppInventory";
import { MetaAppInventoryFromJSON, MetaAppInventoryFromJSONTyped, MetaAppInventoryToJSON } from "./MetaAppInventory";

/**
 *
 * @export
 * @interface AppInventory
 */
export interface AppInventory {
    /**
     *
     * @type {Array<ErrorAppInventory>}
     * @memberof AppInventory
     */
    errors?: Array<ErrorAppInventory>;
    /**
     *
     * @type {MetaAppInventory}
     * @memberof AppInventory
     */
    meta?: MetaAppInventory | null;
    /**
     *
     * @type {Array<AppAppInventory>}
     * @memberof AppInventory
     */
    resources?: Array<AppAppInventory> | null;
}

/**
 * Check if a given object implements the AppInventory interface.
 */
export function instanceOfAppInventory(value: object): value is AppInventory {
    return true;
}

export function AppInventoryFromJSON(json: any): AppInventory {
    return AppInventoryFromJSONTyped(json, false);
}

export function AppInventoryFromJSONTyped(json: any, ignoreDiscriminator: boolean): AppInventory {
    if (json == null) {
        return json;
    }
    return {
        errors: json["errors"] == null ? undefined : (json["errors"] as Array<any>).map(ErrorAppInventoryFromJSON),
        meta: json["meta"] == null ? undefined : MetaAppInventoryFromJSON(json["meta"]),
        resources: json["resources"] == null ? undefined : (json["resources"] as Array<any>).map(AppAppInventoryFromJSON),
    };
}

export function AppInventoryToJSON(value?: AppInventory | null): any {
    if (value == null) {
        return value;
    }
    return {
        errors: value["errors"] == null ? undefined : (value["errors"] as Array<any>).map(ErrorAppInventoryToJSON),
        meta: MetaAppInventoryToJSON(value["meta"]),
        resources: value["resources"] == null ? undefined : (value["resources"] as Array<any>).map(AppAppInventoryToJSON),
    };
}
