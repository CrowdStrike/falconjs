/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from "../runtime";
import type { DomainCloudAccounts } from "./DomainCloudAccounts";
import { DomainCloudAccountsFromJSON, DomainCloudAccountsFromJSONTyped, DomainCloudAccountsToJSON } from "./DomainCloudAccounts";

/**
 *
 * @export
 * @interface DomainCloudScope
 */
export interface DomainCloudScope {
    /**
     *
     * @type {Array<DomainCloudAccounts>}
     * @memberof DomainCloudScope
     */
    accounts?: Array<DomainCloudAccounts>;
    /**
     *
     * @type {string}
     * @memberof DomainCloudScope
     */
    businessImpact?: string;
    /**
     *
     * @type {string}
     * @memberof DomainCloudScope
     */
    businessUnit?: string;
    /**
     *
     * @type {string}
     * @memberof DomainCloudScope
     */
    cid?: string;
    /**
     *
     * @type {Date}
     * @memberof DomainCloudScope
     */
    createdAt?: Date;
    /**
     *
     * @type {string}
     * @memberof DomainCloudScope
     */
    description?: string;
    /**
     *
     * @type {number}
     * @memberof DomainCloudScope
     */
    id?: number;
    /**
     *
     * @type {string}
     * @memberof DomainCloudScope
     */
    name?: string;
    /**
     *
     * @type {Array<string>}
     * @memberof DomainCloudScope
     */
    owners?: Array<string>;
    /**
     *
     * @type {number}
     * @memberof DomainCloudScope
     */
    totalAccounts?: number;
}

/**
 * Check if a given object implements the DomainCloudScope interface.
 */
export function instanceOfDomainCloudScope(value: object): boolean {
    let isInstance = true;

    return isInstance;
}

export function DomainCloudScopeFromJSON(json: any): DomainCloudScope {
    return DomainCloudScopeFromJSONTyped(json, false);
}

export function DomainCloudScopeFromJSONTyped(json: any, ignoreDiscriminator: boolean): DomainCloudScope {
    if (json === undefined || json === null) {
        return json;
    }
    return {
        accounts: !exists(json, "accounts") ? undefined : (json["accounts"] as Array<any>).map(DomainCloudAccountsFromJSON),
        businessImpact: !exists(json, "business_impact") ? undefined : json["business_impact"],
        businessUnit: !exists(json, "business_unit") ? undefined : json["business_unit"],
        cid: !exists(json, "cid") ? undefined : json["cid"],
        createdAt: !exists(json, "created_at") ? undefined : new Date(json["created_at"]),
        description: !exists(json, "description") ? undefined : json["description"],
        id: !exists(json, "id") ? undefined : json["id"],
        name: !exists(json, "name") ? undefined : json["name"],
        owners: !exists(json, "owners") ? undefined : json["owners"],
        totalAccounts: !exists(json, "total_accounts") ? undefined : json["total_accounts"],
    };
}

export function DomainCloudScopeToJSON(value?: DomainCloudScope | null): any {
    if (value === undefined) {
        return undefined;
    }
    if (value === null) {
        return null;
    }
    return {
        accounts: value.accounts === undefined ? undefined : (value.accounts as Array<any>).map(DomainCloudAccountsToJSON),
        business_impact: value.businessImpact,
        business_unit: value.businessUnit,
        cid: value.cid,
        created_at: value.createdAt === undefined ? undefined : value.createdAt.toISOString(),
        description: value.description,
        id: value.id,
        name: value.name,
        owners: value.owners,
        total_accounts: value.totalAccounts,
    };
}
