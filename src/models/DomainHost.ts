/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from "../runtime";
/**
 *
 * @export
 * @interface DomainHost
 */
export interface DomainHost {
    /**
     *
     * @type {boolean}
     * @memberof DomainHost
     */
    adminAvailable?: boolean;
    /**
     *
     * @type {number}
     * @memberof DomainHost
     */
    bandwidthMbps?: number;
    /**
     *
     * @type {boolean}
     * @memberof DomainHost
     */
    behindNat?: boolean;
    /**
     *
     * @type {string}
     * @memberof DomainHost
     */
    ip?: string;
    /**
     *
     * @type {string}
     * @memberof DomainHost
     */
    ipParsed?: string;
    /**
     *
     * @type {string}
     * @memberof DomainHost
     */
    isp?: string;
    /**
     *
     * @type {string}
     * @memberof DomainHost
     */
    osBanner?: string;
    /**
     *
     * @type {number}
     * @memberof DomainHost
     */
    pingMs?: number;
    /**
     *
     * @type {string}
     * @memberof DomainHost
     */
    processorBanner?: string;
    /**
     *
     * @type {number}
     * @memberof DomainHost
     */
    ramGigs?: number;
    /**
     *
     * @type {boolean}
     * @memberof DomainHost
     */
    sslAvailable?: boolean;
    /**
     *
     * @type {string}
     * @memberof DomainHost
     */
    tld?: string;
    /**
     *
     * @type {number}
     * @memberof DomainHost
     */
    uptimeHours?: number;
}

/**
 * Check if a given object implements the DomainHost interface.
 */
export function instanceOfDomainHost(value: object): boolean {
    let isInstance = true;

    return isInstance;
}

export function DomainHostFromJSON(json: any): DomainHost {
    return DomainHostFromJSONTyped(json, false);
}

export function DomainHostFromJSONTyped(json: any, ignoreDiscriminator: boolean): DomainHost {
    if (json === undefined || json === null) {
        return json;
    }
    return {
        adminAvailable: !exists(json, "admin_available") ? undefined : json["admin_available"],
        bandwidthMbps: !exists(json, "bandwidth_mbps") ? undefined : json["bandwidth_mbps"],
        behindNat: !exists(json, "behind_nat") ? undefined : json["behind_nat"],
        ip: !exists(json, "ip") ? undefined : json["ip"],
        ipParsed: !exists(json, "ip_parsed") ? undefined : json["ip_parsed"],
        isp: !exists(json, "isp") ? undefined : json["isp"],
        osBanner: !exists(json, "os_banner") ? undefined : json["os_banner"],
        pingMs: !exists(json, "ping_ms") ? undefined : json["ping_ms"],
        processorBanner: !exists(json, "processor_banner") ? undefined : json["processor_banner"],
        ramGigs: !exists(json, "ram_gigs") ? undefined : json["ram_gigs"],
        sslAvailable: !exists(json, "ssl_available") ? undefined : json["ssl_available"],
        tld: !exists(json, "tld") ? undefined : json["tld"],
        uptimeHours: !exists(json, "uptime_hours") ? undefined : json["uptime_hours"],
    };
}

export function DomainHostToJSON(value?: DomainHost | null): any {
    if (value === undefined) {
        return undefined;
    }
    if (value === null) {
        return null;
    }
    return {
        admin_available: value.adminAvailable,
        bandwidth_mbps: value.bandwidthMbps,
        behind_nat: value.behindNat,
        ip: value.ip,
        ip_parsed: value.ipParsed,
        isp: value.isp,
        os_banner: value.osBanner,
        ping_ms: value.pingMs,
        processor_banner: value.processorBanner,
        ram_gigs: value.ramGigs,
        ssl_available: value.sslAvailable,
        tld: value.tld,
        uptime_hours: value.uptimeHours,
    };
}
