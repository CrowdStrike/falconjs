/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from "../runtime";
import type { RisksVertex } from "./RisksVertex";
import { RisksVertexFromJSON, RisksVertexFromJSONTyped, RisksVertexToJSON } from "./RisksVertex";
import type { RisksEdge } from "./RisksEdge";
import { RisksEdgeFromJSON, RisksEdgeFromJSONTyped, RisksEdgeToJSON } from "./RisksEdge";

/**
 *
 * @export
 * @interface RisksGraph
 */
export interface RisksGraph {
    /**
     *
     * @type {Array<RisksEdge>}
     * @memberof RisksGraph
     */
    edges?: Array<RisksEdge>;
    /**
     *
     * @type {string}
     * @memberof RisksGraph
     */
    errorMsg?: string;
    /**
     *
     * @type {boolean}
     * @memberof RisksGraph
     */
    isFallback?: boolean;
    /**
     *
     * @type {Array<RisksVertex>}
     * @memberof RisksGraph
     */
    vertices?: Array<RisksVertex>;
}

/**
 * Check if a given object implements the RisksGraph interface.
 */
export function instanceOfRisksGraph(value: object): value is RisksGraph {
    return true;
}

export function RisksGraphFromJSON(json: any): RisksGraph {
    return RisksGraphFromJSONTyped(json, false);
}

export function RisksGraphFromJSONTyped(json: any, ignoreDiscriminator: boolean): RisksGraph {
    if (json == null) {
        return json;
    }
    return {
        edges: json["edges"] == null ? undefined : (json["edges"] as Array<any>).map(RisksEdgeFromJSON),
        errorMsg: json["error_msg"] == null ? undefined : json["error_msg"],
        isFallback: json["is_fallback"] == null ? undefined : json["is_fallback"],
        vertices: json["vertices"] == null ? undefined : (json["vertices"] as Array<any>).map(RisksVertexFromJSON),
    };
}

export function RisksGraphToJSON(value?: RisksGraph | null): any {
    if (value == null) {
        return value;
    }
    return {
        edges: value["edges"] == null ? undefined : (value["edges"] as Array<any>).map(RisksEdgeToJSON),
        error_msg: value["errorMsg"],
        is_fallback: value["isFallback"],
        vertices: value["vertices"] == null ? undefined : (value["vertices"] as Array<any>).map(RisksVertexToJSON),
    };
}
