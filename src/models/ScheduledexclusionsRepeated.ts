/* tslint:disable */
/* eslint-disable */
/**
 * CrowdStrike API Specification
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: rolling
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from "../runtime";
/**
 *
 * @export
 * @interface ScheduledexclusionsRepeated
 */
export interface ScheduledexclusionsRepeated {
    /**
     *
     * @type {boolean}
     * @memberof ScheduledexclusionsRepeated
     */
    allDay?: boolean;
    /**
     *
     * @type {string}
     * @memberof ScheduledexclusionsRepeated
     */
    endTime?: string;
    /**
     *
     * @type {string}
     * @memberof ScheduledexclusionsRepeated
     */
    frequency?: string;
    /**
     *
     * @type {Array<number>}
     * @memberof ScheduledexclusionsRepeated
     */
    monthlyDays?: Array<number>;
    /**
     *
     * @type {string}
     * @memberof ScheduledexclusionsRepeated
     */
    occurrence?: string;
    /**
     *
     * @type {string}
     * @memberof ScheduledexclusionsRepeated
     */
    startTime?: string;
    /**
     *
     * @type {Array<string>}
     * @memberof ScheduledexclusionsRepeated
     */
    weeklyDays?: Array<string>;
}

/**
 * Check if a given object implements the ScheduledexclusionsRepeated interface.
 */
export function instanceOfScheduledexclusionsRepeated(value: object): boolean {
    let isInstance = true;

    return isInstance;
}

export function ScheduledexclusionsRepeatedFromJSON(json: any): ScheduledexclusionsRepeated {
    return ScheduledexclusionsRepeatedFromJSONTyped(json, false);
}

export function ScheduledexclusionsRepeatedFromJSONTyped(json: any, ignoreDiscriminator: boolean): ScheduledexclusionsRepeated {
    if (json === undefined || json === null) {
        return json;
    }
    return {
        allDay: !exists(json, "all_day") ? undefined : json["all_day"],
        endTime: !exists(json, "end_time") ? undefined : json["end_time"],
        frequency: !exists(json, "frequency") ? undefined : json["frequency"],
        monthlyDays: !exists(json, "monthly_days") ? undefined : json["monthly_days"],
        occurrence: !exists(json, "occurrence") ? undefined : json["occurrence"],
        startTime: !exists(json, "start_time") ? undefined : json["start_time"],
        weeklyDays: !exists(json, "weekly_days") ? undefined : json["weekly_days"],
    };
}

export function ScheduledexclusionsRepeatedToJSON(value?: ScheduledexclusionsRepeated | null): any {
    if (value === undefined) {
        return undefined;
    }
    if (value === null) {
        return null;
    }
    return {
        all_day: value.allDay,
        end_time: value.endTime,
        frequency: value.frequency,
        monthly_days: value.monthlyDays,
        occurrence: value.occurrence,
        start_time: value.startTime,
        weekly_days: value.weeklyDays,
    };
}
